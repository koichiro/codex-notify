#!/usr/bin/env ruby
# frozen_string_literal: true

require 'bundler/setup'
$LOAD_PATH.unshift(File.expand_path('../lib', __dir__))

require 'codex_notify/hook_cli'

exit(CodexNotify::HookCLI.main(ARGV))
